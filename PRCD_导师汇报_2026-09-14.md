# Optical-to-SAR 旋转目标检测蒸馏：阶段工作汇报与后续研究方向

核对日期：2026-09-14。项目：`ultralytics-8.3.82-distill`。

本报告依据当前代码、运行目录保存的配置和训练 CSV 整理。模块名称表示当前研究设计，不代表已经通过文献比较和实验验证的新颖性贡献。后续方向均为待验证方案。

## 1. 研究目标与当前阶段

研究目标是在训练时利用对齐的光学/SAR 图像，让光学教师向轻量 SAR 学生传递知识，提升 SAR 旋转目标检测精度。部署时仅输入 SAR 图像，不依赖光学图像或大模型。

当前已搭建 PRCD（Prototype-Referenced Cross-Modal Distillation，原型参照跨模态蒸馏）实验框架，包含 SPD、ICD、CRC 三个可配置模块，完成 OGSOD-1.0 上一组 400 轮 SPD+ICD 实验。

阶段结论：框架已能够完成训练，SPD/ICD 代理损失持续改善，但当前组合未提升主要检测指标。SAR baseline 的 mAP50-95 为 55.950%，SPD+ICD 为 55.074%，下降 0.876 个百分点。尚无充分结果判断 SPD、ICD 各自的独立贡献，也没有当前 CRC 完整组合的有效性证据。

后续希望结合视觉语言模型（VLM）、语言语义或视觉基础模型（VFM），在保留轻量 SAR 推理的基础上，研究更有任务针对性的知识迁移。

## 2. 数据与实验划分

| 数据集 | 配置入口 | 类别 | 输入及划分 |
|---|---|---|---|
| OGSOD-1.0 | `dataset/ogsod1_paired.yaml` | 0 bridge、1 harbor、2 storage_tank | 256；train/val，YAML 中 test 复用 val |
| OGSOD-2.0 | `dataset/ogsod2_paired.yaml` | 同上三类 | 256；独立 train/val/test |
| SFOC-1.0-100K-SOA | `dataset/sfoc_paired.yaml` | 0 plane、1 ship | 512；train/test，YAML 中 val 复用 test |

OGSOD 两套数据现位于仓库 `dataset/OGSOD-1.0`、`dataset/OGSOD-2.0` 下。SFOC 配置指向 `/media/mengfanlong/111E18BA111E18BA/数据集/SFOC-1.0-100K-SOA`，通过 manifest 对应切片身份。

OGSOD-1.0 已有 profile 记录：训练图像 14,664 对，标注实例 38,966 个；三类实例数分别为 25,533、3,306、10,127，最大/最小类别频次比约 7.72。此前完整验证包含 3,667 张图像、9,614 个实例。

SFOC 配置已经使用连续标签 0/1。README 记录早期标签 0/3 已重映射到 0/1；本次未重新修改源标签。512 profile 与 OGSOD 256 profile 独立。当前预设 SFOC 教师 best.pt 路径仍不存在，不能宣称已完成该数据集的教师或蒸馏实验。

实验规范问题：复用的 val/test 只能算同一个评估集，不能写成两个独立验证结果。尤其 SFOC 若反复使用 test 调参，应另从训练数据中按原始场景/大图划出验证集，保持最终测试集隔离。切片数据应避免同源场景跨划分泄漏。

## 3. 整体实现

训练过程为：

```text
对齐的光学图像 → 冻结光学教师 → 多尺度特征 → 目标区域表示 / 教师原型
                                              ↓ SPD、ICD、可选 CRC
对应的 SAR 图像 → SAR 学生 → 多尺度特征 → OBB 检测头 → 检测监督

部署：SAR 图像 → 导出的纯学生 OBB 模型 → 类别与旋转框
```

现有 OGSOD-1.0 光学教师实际为 YOLOv8s-OBB-P2，检测 stride 为 4/8/16/32；学生和 baseline 为 YOLOv8n-OBB，stride 为 8/16/32。教师并非与学生同结构、仅输入模态不同的对照。

教师 eval、冻结梯度、no_grad 前向；学生由通用 `yolov8n-obb.pt` 初始化，不从 SAR baseline best.pt 开始。baseline 作为对照，不是蒸馏必需的初始化阶段。

特征通过 OBB 检测头输入 hook 获取，按实际 stride 配对。目前配置选 P3/P4/P5，因此使用共同 stride 8/16/32。教师 P2 没有直接作为蒸馏目标，但教师 neck 的跨层融合可能使其信息间接进入其他尺度，不能说 P2 信息被完全切断。

### 3.1 共用的目标区域构建与特征表示

1. 根据 GT 旋转框，在各特征图上建立连续坐标的局部采样支持。
2. 当前采样网格为 7×7，使用双线性 grid_sample；adaptive 半径由目标尺寸计算并限制在 1–4 个特征格，上下文 margin 为 1。
3. 用旋转框占据掩码区分 core 和 context，core 使用 4×4 子采样估计占据程度；context 可排除同图邻近标注目标。
4. 当前网格沿特征图坐标轴采样，旋转角体现在区域掩码中；它不是已经沿目标长轴旋转对齐的 token 网格。
5. 教师 core/context 池化后拼接，经固定随机投影得到 128 维向量；学生采样特征经可学习 1×1 Conv + GroupNorm + SiLU，再池化、拼接得到 128 维向量；两端进行 L2 归一化。

这些区域操作、投影器和适配器属于实现设计，需要消融验证其作用。7×7 插值输出不等于原始特征具有 7×7 的目标细节；固定随机投影也不是已经学到的语言语义空间。

## 4. 三个现有候选创新模块

### 4.1 SPD：Semantic Prototype Distillation，语义原型蒸馏

动机：光学和 SAR 的逐点特征差异较大，先在类别统计层面对齐，期望迁移更稳定的类别知识。

实现：每个尺度、每个类别维护一个归一化光学教师 EMA 原型，momentum=0.99。学生使用当前 batch 内该类别所有有效实例向量的均值并归一化。损失为学生类别原型与教师 EMA 原型的余弦距离，按有效类别组聚合。

```text
教师当前类别均值 → 更新教师 EMA 原型 p_T(c,l)
学生当前类别均值 → 学生原型 p_S(c,l)
L_SPD = 有效类别/尺度上平均 [1 - cosine(p_S, p_T)]
```

当前三类、三尺度最多有 9 个类别-尺度原型槽。`prototype_relation_enabled=false`，没有额外启用类间原型关系项。SPD 权重为 0.1。

已有实现：`ultralytics/nn/distillation/spd.py`、`prototype_bank.py`。

目前限制：单原型会压缩同类的形状、尺寸、背景差异；均值对齐不保证逐实例判别力或精确定位提升。可学习学生适配器也可能吸收部分对齐任务，需要测量传入检测 backbone/neck 的实际收益。

待讨论扩展：可靠性加权的原型、多原型、视觉-语言共同语义原型。这些尚未实现或验证。

### 4.2 ICD：Instance Configuration Distillation，实例构型蒸馏

动机：在类别中心以外，迁移同类别实例之间的差异关系，补充 SPD 只对齐均值的不足。

实现：在每个尺度、每个类别内选取 batch 中的目标，至少 2 个，最多 64 个。教师实例减去教师 EMA 原型，学生实例减去当前抽样组的归一化均值；将残差归一化，分别构造实例间余弦相似度 Gram 矩阵，计算非对角元素 MSE。

```text
r_T(i) = normalize(z_T(i) - p_T_EMA)
r_S(i) = normalize(z_S(i) - p_S_sample)
G_T(i,j) = dot(r_T(i), r_T(j))
G_S(i,j) = dot(r_S(i), r_S(j))
L_ICD = 有效组上平均 MSE(G_S 非对角项, G_T 非对角项)
```

当前 `pair_direction=false`，不额外对齐配对残差方向；`memory_assisted=false`、`cross_rank_gather=false`。ICD 权重为 0.05。

已有实现：`ultralytics/nn/distillation/icd.py`。

特别说明：当前 ICD 是同类实例之间的关系，可跨 batch 内不同图像；不是同一个目标内部的长轴、边缘或局部 token 关系。此前提出的“改成实例内部定向结构蒸馏”属于新的方法变体，会改变 ICD 定义，需要独立命名/版本和对照。

目前限制：关系可能包含光学特有纹理和背景；教师使用长期 EMA 中心、学生使用当前抽样中心，两个统计基准不完全相同；关系 MSE 下降并不自动意味着 OBB 性能提高。

### 4.3 CRC：Cross-Modal Response Calibration，跨模态响应校准

动机：迁移目标核心与周围上下文之间的响应分配，避免强制两模态逐像素一致。

实现：在共同目标支持上提取 GT 类别 sigmoid 响应；分别对 core/context 求均值，再经温度 softmax 得到区域分布，使用 KL(teacher || student) 对齐。支持两区域或 core/near-context/outer-context 三区域版本。

```text
区域能量 a = [mean(core 响应), mean(context 响应)]
q = softmax(a / temperature)
L_CRC = KL(q_T || q_S)
```

已有实现：`ultralytics/nn/distillation/crc.py`。

当前 400 轮实验 CRC 关闭，权重为 0。现有 CRC 不包含教师 DFL、角度、解码旋转框或教师置信度筛选。训练器虽然提取 DFL/angle，但当前 PRCD 损失未消费这些信息。先前讨论的“升级为定位响应蒸馏”是待研究方案，不能描述为已经完成的第三个创新。

## 5. 总损失及当前具体配置

```text
L = L_det + α(epoch) × [λ_CRC L_CRC + λ_SPD L_SPD + λ_ICD L_ICD]
α(epoch) = min(1, (epoch_index + 1) / 10)
```

实现中检测标量按 batch-size 缩放，蒸馏标量也乘实际 batch-size 后相加，避免两套标量归一化约定不一致。显示的 SPD/ICD 为未乘权重的原始项；`distill_total` 才是加权、warmup 后的蒸馏值。

当前组合 λ_CRC=0、λ_SPD=0.1、λ_ICD=0.05。warmup 10 轮之后蒸馏权重保持不变，没有后期衰减。

当前 `level_assignment=all`：每个有效目标在 P3/P4/P5 都计算蒸馏。跨尺度按有效组数量聚合，并非直接把三层 loss 相加乘三。代码另有 `scale_assigned` 分配方式，但本次完整实验未使用；它按尺寸选择一个尺度，也不保证每一层都有足够目标。

## 6. 已完成的工程改动

| 部分 | 已有功能 | 主要文件 |
|---|---|---|
| 一键训练入口 | 数据集、阶段、蒸馏组合、尺度、初始化选择及路径预检 | `train.py` |
| 数据配置 | 各数据集 paired/optical YAML，显式 label 路径、划分复用说明 | `dataset/*_paired.yaml`、`*_optical.yaml` |
| 配对数据加载 | 按样本 ID/manifest 配对；检查缺失、重复、尺寸；回放 RNG 同步增强并检查 OBB 一致性 | `ultralytics/data/paired_obb_dataset.py` |
| 蒸馏模型/训练器 | 教师冻结、特征捕获、检测损失接入、SAR-only 验证 | `ultralytics/models/yolo/obb/distill_model.py`、`distill_train.py` |
| 模块与配置 | 区域采样、SPD、ICD、CRC、开关与候选消融配置 | `ultralytics/nn/distillation/`、`configs/prcd/ablations/` |
| 数据分析 | 配对完整性、尺寸、类别频率、特征层支持、batch 可行性统计 | `tools/profile_paired_obb_dataset.py`、`dataset_profile.json` |
| 损失诊断 | 无 optimizer step 的 loss/neck 梯度范数 probe | `tools/analyze_distill_loss_scale.py` |
| 训练可观测性 | 主表保留检测及三个蒸馏 loss；诊断写独立 CSV；保存配置/教师 hash/原型状态 | `distill_train.py` |
| 推理导出 | 重建纯 OBB 学生，移除蒸馏模块并比较输出 | `tools/export_sar_student.py` |
| 评估工具 | 普通 OBB 验证与尺寸分层评估工具 | `tools/evaluate_prcd_stratified.py` |

仓库存在配对、区域采样、SPD/ICD/CRC、关闭蒸馏等价性、导出去模块等测试。本次为汇报核对，未重新运行测试，不把“存在测试文件”当作当前版本全部测试通过的证据。

训练 checkpoint 仍可包含适配器/原型状态；需要经过纯学生导出工具才是去除这些训练附加状态的权重。验证不使用教师，故 val 的三个蒸馏 loss 为 0 是设计行为，不代表训练 loss 没生效。

屏蔽 grid_sample 的已知 CUDA 非确定性警告仅影响显示，没有使该操作变为确定性实现。

## 7. 已完成实验与可复核结果

### 7.1 主实验条件

OGSOD-1.0、OBB、imgsz=256、batch=128、400 epochs、seed=0、SGD。lr0/lrf=0.01/0.01，momentum=0.937，weight_decay=0.0005，检测 warmup=3，AMP 开启；mosaic/mixup/copy_paste 均为 0。其余标准几何/颜色增强按保存配置执行。

baseline 与学生使用相同通用 YOLOv8n-OBB 初始化来源。历史 baseline 与当前框架跨时间运行，配置吻合提供可比性依据，但严格归因仍应补做当前代码环境下的关闭蒸馏对照和多种子实验。

### 7.2 第 400 轮原始 CSV 指标

下表所有数值乘 100，以百分数表示；差值单位为百分点。

| 实验 | Precision | Recall | mAP50 | mAP50-95 |
|---|---:|---:|---:|---:|
| 光学教师 YOLOv8s-OBB-P2 | 97.757 | 96.537 | 98.690 | 75.612 |
| SAR baseline YOLOv8n-OBB | 83.567 | 76.310 | 82.777 | 55.950 |
| SAR 学生 + SPD + ICD | 84.866 | 75.781 | 82.792 | 55.074 |
| 学生蒸馏 − SAR baseline | +1.299 | −0.529 | +0.015 | −0.876 |

教师使用光学输入且架构更大、带 P2，其成绩不能直接解释为 SAR 学生可达到的上限，也不能把全部差距归因于光学模态。

结果路径：

- `runs/train/opt-teacher-yolov8s-ogsod-1.0-256-nomosaic/results.csv`
- `runs/train/sar-baseline-raw-yolov8n-ogsod-1.0-256-nomosaic/results.csv`
- `runs/prcd/ogsod1-prcd-yolov8n-p3p4p5-spd-icd/results.csv`

### 7.3 检测损失与蒸馏诊断

| 指标 | baseline 第400轮 | SPD+ICD 第400轮 |
|---|---:|---:|
| train box | 0.97379 | 0.97340 |
| train cls | 0.62252 | 0.62042 |
| train dfl | 1.11014 | 1.11030 |
| val box | 1.25177 | 1.27750 |
| val cls | 0.79907 | 0.81176 |
| val dfl | 1.14041 | 1.14794 |

| 蒸馏诊断 | 第1轮 | 第400轮 |
|---|---:|---:|
| SPD 原始 loss | 0.98527 | 0.03199 |
| ICD 原始 loss | 0.28589 | 0.16602 |
| ICD 关系相关性 | 0.24106 | 0.67525 |
| 原型覆盖率 | 1.0 | 1.0 |
| 教师类内离散度诊断 | 0.41295 | 0.42044 |
| 有效 SPD 类别-尺度组/batch | 8.9739 | 8.9217 |
| 有效 ICD 类别-尺度组/batch | 8.9217 | 8.9217 |

`prototype_variance` 实际为实例与当前教师类别均值的平均余弦距离，不是统计学方差，没有“0.42 即不合格”的通用阈值。coverage=1 只表示所有原型槽已初始化，不代表语义正确或质量达标。

第400轮加权蒸馏量约为 0.1×0.03199+0.05×0.16602=0.01150。与检测显示项总和约 2.704 比较，标量比例约 0.43%；该比例不能代替梯度强度或梯度方向。

此前的初始模型单 batch=128 探针曾测得：检测 neck 梯度范数约18.777，加权 SPD 约0.227，加权 ICD 约0.068。它只能描述一次局部测量，不能代表完整训练，也不能据此断言权重太小就是失败原因。

### 7.4 曲线与耗时

| epoch | baseline mAP50-95 | SPD+ICD mAP50-95 | 差值/百分点 |
|---|---:|---:|---:|
| 50 | 35.530 | 35.941 | +0.411 |
| 100 | 44.588 | 44.682 | +0.094 |
| 119 | 46.307 | 46.480 | +0.173 |
| 200 | 49.907 | 49.366 | −0.541 |
| 300 | 52.768 | 52.205 | −0.563 |
| 400 | 55.950 | 55.074 | −0.876 |

前中期有阶段性优势，后期未保持。两条曲线自身仍在提升，不能简单说“学生训练过头”。也因此，短实验只可用于排查和筛选，不能保证最终400轮同样提升。

CSV 累计时间：baseline 5359.14秒（约1.49小时），SPD+ICD 67852.1秒（约18.85小时），历史记录约12.66倍。这不是同环境受控性能基准，不能把倍数全部归因于蒸馏算法。教师前向、按实例 gather/grid_sample、邻域掩码循环、CPU/GPU 同步均是值得 profiling 的候选开销。

### 7.5 新增短运行与当前入口差异

当前还存在两个短运行：

- `.../ogsod1-prcd-yolov8n-p3p4p5-spd-icd2`：batch128，CSV仅1轮。
- `.../ogsod1-prcd-yolov8n-p3p4p5-spd-icd3`：batch512，CSV仅1轮。

它们不是完整复现实验，也不能用于判断收敛。当前 `train.py` 中 OGSOD-1.0 batch 已为512，尽管旁边旧注释仍写128；400轮历史结论对应保存的 batch128 配置。512会改变每epoch优化步数和类别组统计，不应视作仅提高显存占用的无影响调整。

历史 baseline 的 `args.yaml` 引用通用 `dataset/data.yaml`，该文件现已指向 OGSOD-2.0。此前通过训练样本图和标签统计图的一致性核对支持历史运行确为 OGSOD-1.0，但复现时应使用专属 YAML并存储数据清单/hash。README 部分旧命令仍使用 baseline 初始化或P3单层，与当前主实验不一致。

## 8. 当前结果能说明什么，不能说明什么

能够说明：这一次固定 seed/config 的 SPD+ICD 组合没有提升 mAP50-95；代理任务损失改善没有转化为检测收益。训练损失接近而验证损失更高，提示泛化或任务约束值得调查。

不能据此断定：SPD-only和ICD-only均无效、三尺度本身错误、缺少CRC必然导致失败、增加定位loss一定提升、单原型必然不足，或结果已经排除训练随机性。以上均需控制实验。

优先研究的假设：

1. **知识与目标错位。** SPD/ICD约束池化向量及其关系，没有直接约束框/角度，但特征蒸馏也可能间接改善定位，不能把“无定位loss”视为确定根因。
2. **小目标尺度支持不足。** profile显示P3/P4/P5上目标面积不足1格的比例约39.03%/83.24%/92.98%；全目标三尺度等组聚合可能在粗尺度引入不可靠约束。但语义上下文仍可能有益，需逐层消融。
3. **教师能力与可迁移性差异。** 教师强不保证其优势在SAR输入下可恢复；更大容量和P2是额外混杂因素。
4. **原型与关系目标设计。** 单类均值、教师随机投影、学生适配器、中心统计不对称、无可靠性权重都值得分别检验。
5. **蒸馏强度和时间安排。** 固定权重可能随训练阶段产生不同效果。需测共享参数梯度范数、余弦方向和逐层统计，再决定增减权重或后期衰减。

此前分层评估工具所得整体指标与普通验证存在明显差异，未统一设置并排除实现影响前，不将其“tiny目标下降幅度”写成定论。本报告结果主表统一采用已保存的训练CSV。

## 9. 怎样证明三个模块有贡献

论文没有“只能三个创新点”或“所有模块单独都必须提高固定幅度”的通用硬规则，但每个声称有贡献的模块都应有针对性证据。当前希望达到每个模块独立有益，可把它设为本项目更严格的设计目标，不能预先保证实验单调提升。

建议保持原始SPD/ICD/CRC定义完成基准消融：baseline、SPD、ICD、CRC、SPD+ICD、SPD+CRC、ICD+CRC、Full。再对完整方法做逐个删除模块的对照，以区分独立贡献和交互作用。

采用同环境、同数据、同初始化、同训练预算，候选最终配置至少多种子复核，报告均值/标准差；有条件采用按场景/图像重采样的配对置信区间。不能固定认定0.3或0.5个百分点就是显著，也不能根据seed0调好后仅报告它。

先核对现有同配方baseline，再做少量单因素实验；不要同时换模块定义、batch、教师和输入分辨率，否则无法归因。使用验证集选方案，独立测试集只用于最终评估。

## 10. 引入VLM、语言和VFM的可讨论路线

本节是方案设计，不是已完成工作。以下列出本次查询可核实的较新官方模型资料，作为候选，不声称是截至本日的全领域最新或最佳排名；正式选型还需比较版本、许可、资源、遥感适配与数据合规。

### 10.1 语言增强SPD：把文本作为类别语义参照

可从类别名与专家编写的形状/上下文描述起步，通过图文对齐编码器提取文本向量，与光学区域向量形成语义目标，再传给SAR学生。SigLIP2提供图文编码器候选；生成式VLM可辅助形成描述。Qwen3-VL官方提供图像理解及空间感知能力，并有不同规模模型和微调代码。[SigLIP2官方资料](https://github.com/google-research/big_vision/blob/main/big_vision/configs/proj/image_text/README_siglip2.md)、[Qwen3-VL官方仓库](https://github.com/QwenLM/Qwen3-VL)。

与当前实现的关系：保留原型银行、配对区域、轻量学生，将原型由纯光学统计中心扩展为经验证的视觉-语言语义参照。文本向量与当前128维随机投影不在天然共同空间，必须有对齐机制与对照。

验证重点：标签名称、固定专家描述、VLM生成描述三者分别比较；加入错配文本/随机向量控制，排除增益仅来自额外参数或正则。只有2–3类时，文本信息可能与GT类别重复，价值不能预设。

对微小目标，不应让VLM仅看整张图就生成逐实例描述。应采用GT对应ROI加适度上下文、结构化属性输出、人工抽查和不确定性过滤。推理时不能使用测试实例GT构造输入。

### 10.2 VFM增强ICD：更稳定的实例/局部结构表征

DINOv3是可选的视觉基础特征教师，官方提供预训练模型及特征使用入口。可先在光学ROI离线提取patch特征，评估它是否比当前检测neck更适合表示结构，再决定替换ICD教师表示。[DINOv3官方仓库](https://github.com/facebookresearch/dinov3)。

两种方向应明确区分：保持当前跨实例ICD，仅替换特征来源；或提出新版本实例内部token关系蒸馏。后者不是原ICD的小改动，必须独立验证。

风险：自然图像预训练与遥感有域差，patch尺寸对小目标可能过粗；离线特征缓存必须考虑训练几何增强，否则缓存和GT/学生坐标会错位。冻结骨干、ROI/token缓存可作为成本控制方案，但需实测吞吐。

### 10.3 可靠性/区域增强CRC：语言提示与基础分割模型辅助

SAM3支持基于文本或图像示例的概念提示检测、分割与跟踪，可作为光学区域掩码的候选来源。[SAM3官方资料](https://ai.meta.com/research/sam3/)、[官方实现](https://github.com/facebookresearch/sam3)。

可研究使用更贴近目标形状的可靠掩码构造core/context，或用基础模型与任务教师的一致性筛选可蒸馏区域，改善现有CRC。其输出首先是候选监督，不能当作精确OBB真值；小目标和SAR散射差异仍需检查。

若另外引入DFL/角度/旋转框定位蒸馏，应有统一任务动机与教师质量约束。常见KD项即使放入同一个CRC名字，也不会自动形成新颖贡献。特别是旋转角存在周期及宽高交换等价性，不宜直接对角度数值做普通MSE。

### 10.4 优先建议：一个主线，分步验证

可以向老师讨论“基础模型辅助的语义、关系与区域可靠性跨模态蒸馏”：以现有SPD/ICD/CRC接口为基础，先选择语言或VFM中的一种新增知识源做最小验证，不同时接入所有大模型。

如偏多模态应用与就业技能，优先做语言增强SPD：结构化描述、文本编码、视觉-语言对齐、离线数据管线、错误分析、轻量部署。若定位问题最突出，可优先比较VFM局部特征质量。是否更换第三模块，由消融证据决定，不为凑数量保留无贡献组件。

## 11. 与就业方向的联系

这个方向可以形成具体、可展示的能力：

- 多模态数据工程：配对、标注验证、场景级划分、样本/特征版本管理。
- 基础模型使用：本地VLM/VFM推理、结构化输出、质量评估、离线缓存。
- 模型训练：跨模态适配、对比/蒸馏目标、梯度诊断、按需开展LoRA/SFT。
- 实验与部署：控制消融、多种子、精度/延迟/显存测量、纯SAR学生导出及演示。

目前已经有配对训练、知识蒸馏和学生导出基础；VLM接入、语言对齐、LoRA/SFT均属于后续工作，不能提前写成已掌握的项目成果。若目标是多模态大模型训练岗位，单纯调用VLM生成文本通常不足以展示训练能力，应视需求增加真实适配与评估；并不必为了简历强行增加无效微调。

## 12. 希望与老师讨论的决策

1. 主目标继续是闭集SAR旋转检测，还是正式扩展为开放词汇/文本指代？仅训练期使用文本不等于开放词汇检测；后者需要novel类别划分和相应推理接口。
2. 优先验证语言语义对齐，还是VFM局部结构迁移？哪些具体知识在光学与SAR之间可迁移？
3. 是否先完成原始模块消融，再决定保留、重定义或替换SPD/ICD/CRC？
4. 如何建立SFOC独立验证划分，并将场景级泛化作为第二数据集验证重点？
5. 在可用显存、时间和职业方向约束下，优先做到什么可复核成果：精度增益、可控多模态适配、轻量部署或完整评估平台？

当前最有价值的阶段成果是一个已可运行、可观测、可消融的跨模态OBB研究基础，以及一组明确的负结果。下一步需要用受控实验决定哪些知识和模块值得保留，把后续VLM/VFM能力接入到可检验的问题上。
